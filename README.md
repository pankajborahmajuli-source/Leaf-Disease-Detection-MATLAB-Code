# Leaf-Disease-Detection-MATLAB-Code
Here's a basic MATLAB code that detects diseased areas in a leaf image, calculates the percentage of affected area, and extracts the spots (disease-affected regions) as a separate image.
